version: "3.1"
services:
  api-server:
    image: duchungho/swc-api:446ab5a
    restart: always
    command: "swc s"
    networks:
      - bridge
    ports:
      - 8000:8000
networks:
  bridge:
volumes:
  k8s:
